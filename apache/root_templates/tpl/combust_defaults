[%-  page = {
            title = template.title,
            style = template.style or 'default.html',
            js    = [],
            css   = [],
    };

    # don't include any whitespace from here...
    RETURN;
-%]

